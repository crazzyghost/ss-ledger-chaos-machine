#!/usr/bin/env bash

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# shellcheck source=./_local-dev-common.sh
source "${SCRIPT_DIR}/conf.sh"

printenv | grep CHAOS_MACHINE

printf "\n"
printf "executing script...\n"

ITEM_SEQUENCE_ARG=$1

BATCH_ID=${CHAOS_MACHINE_CURRENT_DISBURSEMENT_BATCH_ID} \
ITEM_SEQUENCE=${ITEM_SEQUENCE:${ITEM_SEQUENCE_ARG:1}} \
PRINCIPAL_AMOUNT=${CHAOS_MACHINE_CURRENT_DISBURSEMENT_BATCH_ITEM_AMOUNT} \
FEE_AMOUNT=${CHAOS_MACHINE_CURRENT_DISBURSEMENT_BATCH_ITEM_FEE_AMOUNT} \
VIRTUAL_ACCOUNT_ID=${SOURCE_VA_ID:-${CHAOS_MACHINE_ORGANIZATION_VIRTUAL_ACCOUNT_ID}} \
MERCHANT_ID=${MERCHANT_ID:-${CHAOS_MACHINE_ORGANIZATION_ID}} \
RESERVATION_ID=${RESERVATION_ID:-${CHAOS_MACHINE_CURRENT_RESERVATION_ID}} \
FEE_VA_ID=${FEE_VA_ID:-${CHAOS_MACHINE_FEE_REVENUE_ACCOUNT_ID}} "${CHAOS_MACHINE_SCRIPT_BIN}/publish-disbursement-batch-item-completed.sh"